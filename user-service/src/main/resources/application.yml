server:
  port: 8092

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: user-service
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus