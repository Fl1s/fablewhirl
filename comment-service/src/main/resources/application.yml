server:
  port: 8085

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  threads:
    virtual:
      enabled: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/fablewhirl-realm


  application:
    name: thread-service

  data:
    mongodb:
      uri: mongodb://admin:secret@localhost:27020/comment_data

minio:
  bucket:
    name: users
  url: http://localhost:9000
  root-user: admin
  root-password: secret123

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8095/eureka/


management:
  endpoints:
    web:
      exposure:
        include: prometheus

keycloak:
  jwks-certs: http://localhost:8180/realms/fablewhirl-realm/protocol/openid-connect/certs