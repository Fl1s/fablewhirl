server:
  port: 8094

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  cache:
    type: redis
  application:
    name: thread-service
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus