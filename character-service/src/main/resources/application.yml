server:
  port: 8083

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  data:
    mongodb:
      uri: mongodb://admin:secret@mongo:27017/character_data
  application:
    name: character-service

  kafka:
    producer:
      bootstrap-servers: kafka:9092

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-sevice:8180/realms/fablewhirl-realm

keycloak:
  jwks-certs: http://auth-service:8080/realms/fablewhirl-realm/protocol/openid-connect/certs

eureka:
  instance:
    hostname: eureka-server
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8095/eureka/


management:
  endpoints:
    web:
      exposure:
        include: prometheus