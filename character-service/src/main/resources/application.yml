server:
  port: 8083

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  data:
    mongodb:
      uri: mongodb://admin:secret@localhost:27018/character_data
  docker:
    compose:
      file: ./character-service/docker-compose.yaml
  application:
    name: character-service

  kafka:
    producer:
      bootstrap-servers: localhost:9092

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/fablewhirl-realm

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8095/eureka/

keycloak:
  jwks-certs: http://localhost:9090/realms/fablewhirl-realm/protocol/openid-connect/certs

management:
  endpoints:
    web:
      exposure:
        include: prometheus