server:
  port: 8091

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: auth-service
  threads:
    virtual:
      enabled: true

keycloak:
  realm: fablewhirl-realm
  resource: auth-service
  credentials:
    client-id: auth-service
    secret: MGZFsFHFgfhxk36PTzenG3G5GkJwv0By
  use-resource-role-mappings: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus